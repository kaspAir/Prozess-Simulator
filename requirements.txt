Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
email-validator==2.2.0
# Server laeuft Python 3.9 -> keine harte 1.2.2-Pin (braucht 3.10). Untergrenze
# hebt die CVE-anfaellige 1.0.1 an; CI/3.12 zieht die neueste (CVE-frei),
# der 3.9-Server die neueste 3.9-kompatible Version.
python-dotenv>=1.1.1
gunicorn>=22.0.0
# XXE-sicheres Parsen des (nutzererstellten) BPMN-XML
defusedxml>=0.7.1

# Optionale DB-Treiber (Standard ist SQLite):
PyMySQL==1.1.1          # MariaDB / MySQL  (mysql+pymysql://…)
psycopg2-binary==2.9.12  # PostgreSQL      (postgresql+psycopg2://…)
